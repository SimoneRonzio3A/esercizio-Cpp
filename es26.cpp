#include <cstdlib>
#include <ctime> 
using namespace std;
int main() {
    String nome;
    String rispostagiusta="anna";
    srand(time(0));
    double* nome = new double[nome.length()]; 
    for (int i = 0; i < nome.length(); i++) {
        nome[i] = rand() % 100 + 1;
    }
    for (int i = 0; i < nome.length(); i++) {
        if(rispostagiusta =="anna")
    }